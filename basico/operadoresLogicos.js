// Operador && (Y lógico)
// Retorna true si ambos operandos son verdaderos
let resultadoY = (5 > 3) && (10 > 5); // true porque ambas comparaciones son verdaderas
console.log(resultadoY); // Imprime: true

// Operador || (O lógico)
// Retorna true si al menos uno de los operandos es verdadero
let resultadoO = (5 > 3) || (10 < 5); // true porque la primera comparación es verdadera
console.log(resultadoO); // Imprime: true

// Operador ! (NO lógico)
// Retorna true si el operando es falso, y false si es verdadero
let resultadoNo = !(5 > 3); // false porque 5 es mayor que 3
console.log(resultadoNo); // Imprime: false