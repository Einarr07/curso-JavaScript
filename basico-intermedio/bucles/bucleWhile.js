// Inicializamos la variable numero en 0
let numero = 0;

// Bucle while que se ejecutará mientras numero sea menor que 10
while (numero < 10) {
    // Imprimimos el valor actual de numero en el documento
    document.write(`${numero} <br>`);

    // Incrementamos numero en 1 en cada iteración
    numero++;
}
