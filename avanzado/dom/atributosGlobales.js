const global = document.querySelector(".global");

// Cambiar el color del texto y el fondo usando estilos
global.style.color = "orange";
global.style.backgroundColor = "black";

// Modificar atributos globales (comentados para referencia)
// global.setAttribute("contentEditable", "true"); // Hacer el texto editable
// global.setAttribute("dir", "rtl"); // Dirección del texto (derecha a izquierda)
// global.setAttribute("tabindex", "0"); // Habilitar el enfoque con tabulación
// global.setAttribute("title", "atributos globales"); // Tooltip al pasar el mouse
